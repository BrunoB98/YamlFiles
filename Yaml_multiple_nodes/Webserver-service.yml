apiVersion: v1
kind: Service
metadata:
  name: webserver
  labels:
    app: webserver
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
  selector:
    app: webserver
    tier: frontend
  type: LoadBalancer
